# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: signalr-ai-streaming
metadata:
    template: azd-init@1.9.6
services:
    AIStreaming:
        project: src/AIStreaming
        host: containerapp
        language: dotnet
